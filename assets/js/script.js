new Freezeframe('.freezeframe1', {
  trigger: 'hover',
  overlay: false
});

new Freezeframe('.freezeframe2', {
  trigger: 'click',
  overlay: true
});

new Freezeframe('.freezeframe3', {
  trigger: 'hover',
  overlay: true
});